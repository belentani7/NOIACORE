import{describe,expect,it}from"vitest";import{driftSeverity,envelopeReadiness,evaluateModelGuard,scrubModelText}from"./model-guard-utils";describe("model guard",()=>{it("passes a safe structured response",()=>expect(evaluateModelGuard({prompt:"hi"},{answer:"hello",source:"docs"},["citation"]).result).toBe("pass"));it("blocks raw credentials",()=>expect(evaluateModelGuard({token:"abc"},{answer:"x"},[]).result).toBe("blocked"));it("fails citation checks",()=>expect(evaluateModelGuard({prompt:"hi"},{answer:"hello"},["citation"]).triggeredChecks).toContain("MG-2A-CITATION"));it("classifies drift",()=>expect(driftSeverity(100,130,.1)).toBe("critical"));it("creates ready envelope only for approved clear high pass rate",()=>expect(envelopeReadiness(.98,"clear","approved")).toBe("ready"));it("scrubs secrets from summaries",()=>expect(scrubModelText({apiKey:"abc"})).toContain("[REDACTED]"));});
